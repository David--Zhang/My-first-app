<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.damoyishi.developertraining">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">

        <activity
            android:name="MainActivity"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/MainActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="DisplayMessageActivity"
            android:label="@string/title_activity_display_message"
            android:parentActivityName=".MainActivity"
            android:theme="@style/DisplayMessageActivityTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.damoyishi.developertraining.MainActivity" />
        </activity>

        <activity
            android:name=".FragmentDemo"
            android:label="@string/title_activity_Fragment_demo"
            android:parentActivityName=".DisplayMessageActivity"
            android:theme="@style/DisplayMessageActivityTheme">
        </activity>

        <activity
            android:name=".SharedReferenceTest"
            android:label="@string/title_activity_SharedPreferenceTest"
            android:parentActivityName=".MainActivity"
            android:theme="@style/DisplayMessageActivityTheme">
        </activity>

    </application>

</manifest>
